{
  "name": "@ganova/database",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "generate": "drizzle-kit generate:sqlite",
    "migrate": "echo \"Run D1 migrations from the repo root: npx wrangler d1 migrations apply office-db --local|--remote\" && exit 1"
  },
  "dependencies": {
    "drizzle-orm": "^0.30.10"
  },
  "devDependencies": {
    "drizzle-kit": "^0.20.14",
    "typescript": "^5.0.0"
  }
}
